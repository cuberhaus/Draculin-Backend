services:
  backend:
    build: .
    ports:
      - "8889:8000"
    environment:
      - DJANGO_SETTINGS_MODULE=Draculin.settings
      # Phase 14 / Option A — propagated by PersonalPortfolio/.env.shared
      - SENTRY_DSN=${SENTRY_DSN_DOCKER:-${SENTRY_DSN:-}}
      - SENTRY_ENVIRONMENT=${SENTRY_ENVIRONMENT:-local-dev}
      - SENTRY_TRACES_SAMPLE_RATE=${SENTRY_TRACES_SAMPLE_RATE:-1.0}
      - SENTRY_RELEASE=${SENTRY_RELEASE:-local-dev}
      - PYTHONUNBUFFERED=1
    extra_hosts:
      - "host.docker.internal:host-gateway"

  frontend:
    build:
      context: ../Draculin-Front
      network: host
    ports:
      - "8890:80"
    depends_on:
      - backend
